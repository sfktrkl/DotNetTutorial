@{
    // _ViewImports
    // To create a centralized place for all the
    // directives used in the application.
    // Scope of this file is the current folder
    // and its sub folders just like _ViewStart.

    // Example directives,
    // addTagHelper
    // removeTagHelper
    // tagHelperPrefix

    // using
    // model

    // inherits
    // inject

    @using Tutorial.Models;
}

@{ 
    // Tag helpers are used to render server side code
    // on a Razor file to create and render HTML elements.
    // It is possible creating form, image and link tags.
    // First set the scope for Tag helpers.
    // Then use @addTagHelper and @removeTagHelper directives.
    // It can be used in a single view or in _ViewImports.
    @addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
}
