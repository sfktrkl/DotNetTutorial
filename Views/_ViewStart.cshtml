@{
    // To execute a common code before executing view file.
    // This will be executed before the view, so it can be 
    // overriden during the view easily.
    // Also, each folder can contain another _ViewStart
    // file, so each controller can use different a file.
    Layout = "_Layout";
}
